
function DogInfo(){
    let getdog = prompt("Do you have a dog?");
    if (getdog === "yes") {
        document.write("I own a dog too!");
    } else 
    alert ("this may not be the page for you. Please get a dog and come back");
    return; 
  
}

DogInfo();
